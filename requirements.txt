streamlit
langchain-community
sentence-transformers
transformers
torch # The version *with* CUDA support (e.g., torch==2.2.0+cu121 - check Streamlit Cloud docs for recommended version)
faiss-cpu # Use faiss-gpu if you built the index with GPU, otherwise cpu is fine
bitsandbytes
accelerate
pynvml # (Optional, for better GPU info in sidebar)
